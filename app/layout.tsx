import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Auto Škola Zebra | Profesionalna obuka vožnje",
  description: "Auto Škola Zebra nudi profesionalnu obuku vožnje sa iskusnim instruktorima i modernim vozilima. Visoka stopa prolaznosti na ispitu.",
  keywords: "auto škola, obuka vožnje, vozački ispit, B kategorija, A kategorija, Zebra",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="sr">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
